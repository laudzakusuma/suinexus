{
  "name": "suinexus-monorepo",
  "private": true,
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "build:contracts": "pnpm --filter contracts build"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}